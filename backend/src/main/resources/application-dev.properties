# Dev Profile - Using Remote MySQL
spring.datasource.url=${DB_URL:jdbc:mysql://ceny4h.h.filess.io:3307/TradeX_App_vaporfuel?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:TradeX_App_vaporfuel}
spring.datasource.password=${DB_PASSWORD:44c8d55287186f3844b55c0b36a4d50c312359f4}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# HikariCP - Stay within the remote DB's 5-connection limit
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000
spring.datasource.hikari.connection-timeout=10000

# Truncate and re-seed the remote database on next startup
tradex.seed.reset-db=false

# Local attachment storage
tradex.storage.location=${TRADEX_STORAGE_LOCATION:./data/attachments}
tradex.storage.access-endpoint=${TRADEX_STORAGE_ACCESS_ENDPOINT:https://example.com/api/storage}

# CORS — website + Flutter Web / emulator / LAN device
tradex.cors.allowed-origin-patterns=http://localhost,http://localhost:*,http://127.0.0.1,http://127.0.0.1:*,https://localhost,https://localhost:*,http://10.0.2.2,http://10.0.2.2:*,http://192.168.*:*,http://10.*:*,https://tradenows.com,https://www.tradenows.com,https://*.tradenows.com
